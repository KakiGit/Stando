## 1. Implementation
- [x] 1.1 Apply semantic CSS class names to each widget rendered by `SearchWindow`, covering the window, container boxes, search entry, AI toggle/indicator, scrolled container, list box, rows, and row labels so user styles can target them.
- [x] 1.2 Expand `assets/style.css` with matching class-based rules for backgrounds, borders, and typography (including a `search-result-label` text rule) to document the default look and provide a reference for overrides.
- [x] 1.3 Update the README (or other user docs) to explain where the custom `style.css` lives and list the available selectors/classes for styling the window components and text.

## 2. Validation
- [x] 2.1 `cargo fmt`
- [x] 2.2 `cargo test`
- [x] 2.3 `cargo clippy`
